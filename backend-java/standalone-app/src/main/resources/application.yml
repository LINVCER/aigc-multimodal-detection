# ============================================================
# AIGC 多模态检测 · 独立 Spring Boot 配置
# ============================================================

server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: paperaigc-detect

  profiles:
    active: dev

  # ---------- 数据源 ----------
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/paperaigc?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root

  # ---------- 文件上传 ----------
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  # ---------- Jackson ----------
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    default-property-inclusion: non_null

# ============================================================
# MyBatis-Plus
# ============================================================
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# ============================================================
# 平台业务配置
# ============================================================
platform:
  inference:
    host: ${INFERENCE_HOST:localhost}
    port: ${INFERENCE_PORT:8000}   # 对齐 deploy/inference-python 本地默认端口
    deadline-seconds: 10

  storage:
    # 本地文件存储根目录（LocalFileSystemStorageService 使用）
    local-path: ${STORAGE_LOCAL_PATH:./uploads}

# ============================================================
# Sa-Token（注解驱动；当前业务全用 @SaIgnore 放通，认证由业务层管理）
# ============================================================
sa-token:
  token-name: Authorization
  timeout: 86400
  active-timeout: -1
  is-concurrent: true
  is-share: true
  token-style: uuid
  is-log: false

# ============================================================
# 健康检查
# ============================================================
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

# ============================================================
# 日志
# ============================================================
logging:
  level:
    com.paperaigc: DEBUG
    org.springframework.web: INFO
    com.baomidou.mybatisplus: INFO
